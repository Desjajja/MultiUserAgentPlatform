---
name: experiment_card
description: 专门用于生成和锁定【实验卡片】。当用户确认执行计划（如说”确认计划”、”生成卡片”、”开始存档”）时触发。
metadata:
  openclaw:
    emoji: “🗃️”
    requires:
      bins: [“lark-cli”]
---

# 小环·实验卡片生成规范

## 运行流程
1. **数据捕获**：从对话上下文中提取【任务 3】生成的任务树。
2. **生成卡片**：构造一个结构化的实验卡片（JSON/Markdown 格式）。
3. **数据库模拟**：提示用户该卡片已分配唯一编号（例如：EXP-202410-001）并锁定参数。

## 实验卡片必备字段
请务必包含以下核心字段：
- **卡片 ID**：EXP-{日期}-{序列}
- **目标与成功判据**：明确 pH 达到 8.5 且锂浓度变化可测量。
- **变量控制**：5 组梯度浓度。
- **SOP 步骤快照**：将任务树压缩为精炼的操作规程。
- **数据归档路径**：定义日志和采样数据的存放位置（如 `/data/exp_logs/`）。

## 结果加工要求
1. **统一标题**：回复第一行必须是：**📑 小环·实验卡片已锁定 (v1.1)**。
2. **正式感**：使用代码块展示卡片的 JSON 结构，方便后台记录。
3. **安全确认**：最后必须进行一次“安全自检问询”：
   - “所有安全防护已就绪吗？”
   - “192.168.1.106 的气路系统是否已开启？”
4. **触发动作**：询问用户：“卡片已入库。现在是否进入【执行监控模式】？”

## 云端存档（lark-base + lark-doc）

卡片生成后，执行以下步骤将其同步至飞书：

### 1. 写入飞书多维表格（lark-base）
```bash
# 将实验卡片核心字段写入多维表格，便于后续查询和统计
lark-cli base +record-upsert --base-token <EXP_BASE_TOKEN> --table-id <EXP_TABLE_ID> \
  --json '{“实验编号”: “<EXP_ID>”, “状态”: “进行中”, “目标”: “<目标>”, “日期”: “<日期>”}'
```

### 2. 创建飞书云文档（lark-doc）
```bash
# 将完整卡片 Markdown 内容保存为飞书云文档
lark-cli docs +create --title “<EXP_ID>_Card” --markdown “<卡片 Markdown 内容>”
```

两步均完成后，告知用户文档链接，并提示进入监控模式。

## 注意事项
- 卡片一旦生成，标志着实验参数已”锁定”，不得随意更改。
- 必须包含版本号，如果用户修改了计划，版本号需升级为 v1.2。
- lark-base 写入失败不阻断流程，记录错误后继续。

## Result Contract
- `contract_version`: `v1`
- `kind`: `artifacts`
- `status=ok` 条件：`file` 已生成并包含完整实验卡片；如附带 `text`，可作为同步摘要
- `status=partial` 条件：主卡片文件已生成，但可选摘要或外部登记信息缺失；仍保留完整卡片产物
- `status=failed` 条件：实验卡片无法生成，或关键字段缺失导致无法锁定
- `required_outputs`：`file`
- `optional_outputs`：`text`
- `execution_failure_examples`：任务树提取失败、卡片关键字段缺失、卡片文件未生成
- `delivery_hints`：`file` 是主交付物，`text` 仅作用户可读摘要；飞书同步失败由 Harness 记为 `delivery_failure`
