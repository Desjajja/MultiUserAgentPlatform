---
name: experiment_archive
description: 实验总结与数据归档。当用户说”整理记录”、”生成报告”、”实验结束”、”归档数据”时触发。
metadata:
  openclaw:
    emoji: “📂”
    requires:
      bins: [“lark-cli”]
---

# 小环·实验报告与归档规范

## 运行流程
1. **数据封存**：执行 `python3 {baseDir}/bridge.py`。
2. **内容汇总**：回顾整个会话，提取用户需求、机器人执行关键点和异常诊断结论。

## 结果加工要求（实验报告草稿）
请在飞书生成一份正式的 **Markdown 格式报告**，包含以下板块：

1. **基本信息**：
   - 实验编号：**EXP-2026-0304-001**
   - 状态：✅ 已完成（归档至本地磁盘）
2. **执行复盘**：
   - 简述流程：从 T1 准备到 T5 采样的完成情况。
   - 重点记录：例如“T4 阶段 pH 成功控制在 8.5，耗时 22 分钟”。
3. **数据清单**：
   - 列出已保存到桌面的文件（如 `raw_data.csv`, `ph_curve.png`）。
4. **异常汇总**：
   - 如果发生过任务 7 中的异常，必须在此体现原因和修正方案。
5. **改进建议（Next Step）**：
   - 基于本次实验结果，建议下一次实验可以尝试的变量（如：增加搅拌速度或改变萃取剂浓度）。

## 飞书云端归档

报告生成后，同步至飞书：

### 1. 创建飞书云文档（报告正文）
```bash
lark-cli docs +create --title “<EXP_ID>_Report_{日期}” --markdown “<报告 Markdown 内容>”
```

### 2. 上传原始数据文件至飞书云盘（可选）
```bash
# 上传 CSV、图表等原始数据文件
lark-cli drive +upload --file <本地文件路径> --folder-token <EXP_FOLDER_TOKEN>
```

### 3. 更新多维表格实验状态
```bash
lark-cli base +record-upsert --base-token <EXP_BASE_TOKEN> --table-id <EXP_TABLE_ID> --record-id <record_id> \
  --json '{“状态”: “已完成”, “报告链接”: “<doc_url>”}'
```

## 注意事项
- 报告必须具有”可重复性”指导价值，不仅仅是流水账。
- **严禁**直接打印 JSON 原始代码，要转化成整洁的表格或引用块。
- 结尾告知用户飞书文档链接，并询问：”需要我现在为您生成**日报**发送至团队群吗？”
- lark-doc 创建失败时回退本地存档，不中断流程。

## Result Contract
- `contract_version`: `v1`
- `kind`: `artifacts`
- `status=ok` 条件：实验报告全部五个板块完整生成，file 产物已创建（lark-doc 或本地文件）
- `status=partial` 条件：报告正文生成，但 lark-doc 创建失败；已回退本地存档，file 产物仍存在
- `status=failed` 条件：bridge.py 执行失败，或会话历史不足以生成有效报告，且无本地文件降级
- `required_outputs`: `text`（报告正文），`file`（lark-doc 链接或本地归档路径）
- `optional_outputs`: `forward`（通知用户进入日报生成流程）
- `execution_failure_examples`: bridge.py 超时或返回空数据、会话内无实验记录可归档
- `delivery_hints`: text 和 file 优先交付；lark-doc 失败时 file 降级为本地路径，Harness 记录为 delivery_failure 而非 execution_failure；forward 仅作为进入 daily-report 的触发提示